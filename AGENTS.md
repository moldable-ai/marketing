# Agent Instructions

## Type Checking
Always run type checks & linting before finishing a task that involves code changes.

You can run them using:
```bash
pnpm check-types
pnpm lint
pnpm format
```

If you introduce type errors, fix them before summarizing your work.
